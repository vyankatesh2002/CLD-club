# TODO: Separate CSS & JS into Dedicated Files

## Task List

### 1. index.html
- [x] Already links to `css/index.css` and `js/index.js`
- [ ] Remove the massive duplicated inline `<style>` block
- [ ] Remove the duplicate HTML content (everything is written twice in the file)

### 2. About.html
- [ ] Create `css/about.css` (extract inline `<style>`)
- [ ] Create `js/about.js` (extract inline `<script>`)
- [ ] Update About.html to link external CSS & JS

### 3. Mentors.html
- [ ] Create `css/mentors.css` (extract inline `<style>`)
- [ ] Create `js/mentors.js` (extract inline `<script>`)
- [ ] Update Mentors.html to link external CSS & JS

### 4. Programs.html
- [ ] Create `css/programs.css` (extract inline `<style>`)
- [ ] Create `js/programs.js` (extract inline `<script>`)
- [ ] Update Programs.html to link external CSS & JS

### 5. confidence.html
- [ ] Create `css/confidence.css` (extract inline `<style>`)
- [ ] Create `js/confidence.js` (extract inline `<script>`)
- [ ] Update confidence.html to link external CSS & JS

